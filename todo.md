# TODO

- Update platform identifiers to your final org domain before release:
  - Android `applicationId` / `namespace`
  - iOS `PRODUCT_BUNDLE_IDENTIFIER`
  - Any store-facing app IDs
- Run `/init` after setting the final platform IDs.
